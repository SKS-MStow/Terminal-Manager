# TerminalManager-iOS

SwiftUI app shell for Terminal Manager.

The package in `Sources/TerminalManagerCore` can build under Command Line Tools. A runnable iOS target still needs full Xcode selected and an Xcode project or workspace generated around these sources.
