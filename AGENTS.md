# Agent Instructions

This repo is **Terminal Manager**, not Agent Manager.

- Do not reuse, rename, or import code from `/Users/mark/Github/Agent-Manager` unless the user explicitly asks.
- Keep the product name `Terminal Manager`.
- Keep the terminal raw; any AI compaction belongs in a separate sidecar view.
- Prefer tmux/Mosh/SSH primitives over a Mac daemon for MVP.
- Do not add a hosted relay or cloud backend for MVP.
